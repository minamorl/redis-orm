machine:
  environment:
    TEST_REDIS_PORT: 34352
  pre:
    - redis-server --port 34352 &
  python:
    version: 3.5.0

dependencies:
  pre:
    - pip install tox --upgrade
    - pip install --upgrade setuptools

test:
  override:
    - tox
